{
  "name": "dialogflow-whatsapp-bot",
  "version": "1.0.0",
  "main": "index.js",  // Arquivo principal
  "scripts": {
    "start": "node index.js",  // Comando de inicialização
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "axios": "^1.6.2",
    "body-parser": "^1.20.2"
  }
}
